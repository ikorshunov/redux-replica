{
  "name": "redux-replica",
  "packageManager": "yarn@4.7.0",
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.6",
    "typescript": "^5.8.2"
  },
  "main": "dist/index.js",
  "scripts": {
    "start": "yarn test --watch",
    "test": "jest",
    "check": "biome check ./src",
    "clean": "rm -rf ./dist",
    "build": "yarn test && yarn check && yarn clean && tsc"
  }
}
